[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "omnivoice-gguf-dummy"
version = "0.1.0"
description = "Dummy python package for Dockerfile compatibility"
dependencies = []
