[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "orion-stt-whisper"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "python-multipart",
    "numpy",
    "faster-whisper",
    "websockets",
    "python-dotenv",
]

[tool.setuptools]
py-modules = []
packages = []
